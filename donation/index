<!doctype html>
<p> a paragraph</p>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Donate</title>
  <style>
    :root{
      --bg:#0b0f14;
      --card:#121924;
      --text:#e8eef7;
      --muted:#a7b3c6;
      --btn:#1f6feb;
      --btn2:#18a34a;
      --ring:rgba(255,255,255,.12);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      display:grid;
      place-items:center;
      background:radial-gradient(1200px 600px at 20% 0%, #152238 0%, var(--bg) 55%);
      color:var(--text);
      font:16px/1.45 system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      padding:24px;
    }
    .card{
      width:min(560px, 100%);
      background:linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
      border:1px solid var(--ring);
      border-radius:18px;
      padding:22px;
      box-shadow:0 20px 60px rgba(0,0,0,.45);
      backdrop-filter: blur(8px);
    }
    h1{margin:0 0 6px; font-size:24px}
    p{margin:0 0 16px; color:var(--muted)}
    .img-slot{
      width:100%;
      aspect-ratio: 16 / 9;
      border-radius:14px;
      border:1px dashed rgba(255,255,255,.25);
      display:grid;
      place-items:center;
      color:rgba(255,255,255,.65);
      background:
        linear-gradient(135deg, rgba(255,255,255,.06), rgba(255,255,255,.02));
      margin:14px 0 18px;
      overflow:hidden;
    }
    .img-slot img{
      width:100%;
      height:100%;
      object-fit:cover;
      display:block;
    }
    .grid{
      display:grid;
      gap:12px;
    }
    .btn{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      padding:14px 14px;
      border-radius:14px;
      border:1px solid var(--ring);
      text-decoration:none;
      color:var(--text);
      background:rgba(255,255,255,.04);
      transition:transform .08s ease, background .2s ease, border-color .2s ease;
    }
    .btn:hover{transform:translateY(-1px); background:rgba(255,255,255,.07); border-color:rgba(255,255,255,.18)}
    .btn:active{transform:translateY(0px)}
    .tag{
      font-size:12px;
      padding:6px 10px;
      border-radius:999px;
      border:1px solid var(--ring);
      color:rgba(255,255,255,.8);
      white-space:nowrap;
    }
    .primary .tag{background:rgba(31,111,235,.18); border-color:rgba(31,111,235,.35)}
    .green .tag{background:rgba(24,163,74,.18); border-color:rgba(24,163,74,.35)}
    .mono{
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
      font-size:13px;
      color:rgba(255,255,255,.82);
      word-break:break-all;
    }
    .small{
      margin-top:14px;
      font-size:12px;
      color:rgba(255,255,255,.55);
    }
    button.copy{
      appearance:none;
      border:1px solid var(--ring);
      background:rgba(255,255,255,.04);
      color:var(--text);
      border-radius:10px;
      padding:10px 12px;
      cursor:pointer;
      font-weight:600;
    }
    button.copy:hover{background:rgba(255,255,255,.07)}
    .row{
      display:flex;
      gap:10px;
      align-items:center;
      flex-wrap:wrap;
      margin-top:10px;
    }
  </style>
</head>
<body>
  <main class="card">
    <h1>Support / Donate</h1>
    <p>Thanks for helping out. Choose a method below. 💛</p>

    <!-- Image placeholder: replace with a real <img src="..."> if you want -->
    <div class="img-slot" aria-label="Image placeholder">
      <!-- Option A: keep placeholder text -->
      <div>IMAGE / BANNER GOES HERE</div>

      <!-- Option B: use an image (uncomment and set src) -->
      <!-- <img src="your-image.jpg" alt="Donation banner"> -->
    </div>

    <section class="grid" aria-label="Donation links">
      <!-- CASH APP -->
      <a class="btn green" href="https://cash.app/$YourCashtag" target="_blank" rel="noopener">
        <div>
          <div style="font-weight:700">Cash App</div>
          <div class="mono">$YourCashtag</div>
        </div>
        <span class="tag">Open</span>
      </a>

      <!-- BITCOIN: opens wallet app if available -->
      <a class="btn primary" href="bitcoin:bc1YOURBTCADDRESSHERE" title="Open in wallet">
        <div>
          <div style="font-weight:700">Bitcoin (BTC)</div>
          <div class="mono" id="btcText">bc1YOURBTCADDRESSHERE</div>
        </div>
        <span class="tag">Wallet</span>
      </a>
    </section>

    <div class="row" aria-label="Copy buttons">
      <button class="copy" type="button" data-copy="bc1YOURBTCADDRESSHERE">Copy BTC address</button>
      <button class="copy" type="button" data-copy="$YourCashtag">Copy Cash App</button>
    </div>

    <div class="small">
      Tip: If you want a QR code, you can drop in an image above (or generate a BTC QR and place it there).
    </div>
  </main>

  <script>
    // Tiny copy-to-clipboard helper
    document.querySelectorAll("button.copy").forEach(btn => {
      btn.addEventListener("click", async () => {
        const text = btn.getAttribute("data-copy") || "";
        try {
          await navigator.clipboard.writeText(text);
          const old = btn.textContent;
          btn.textContent = "Copied ✓";
          setTimeout(() => (btn.textContent = old), 1000);
        } catch {
          // fallback: select text in prompt
          window.prompt("Copy this:", text);
        }
      });
    });
  </script>
</body>
</html>
